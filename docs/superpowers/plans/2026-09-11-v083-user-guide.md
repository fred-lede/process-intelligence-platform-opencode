# v0.8.3 使用說明 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 新增依目前頁面與子功能切換的三語言使用說明視窗。

**Architecture:** 在既有 App 導航狀態上建立 guide key，使用共用 Modal 元件與 i18n key 組合內容；說明資料維持前端本地化，不改後端分析流程。

**Tech Stack:** React, TypeScript, Ant Design, i18next。

**Spec:** `docs/superpowers/specs/2026-09-11-v083-user-guide-design.md`

## Global Constraints

- 版本目標：v0.8.3。
- 僅支援 zh-TW、en、es-MX。
- 不修改統計計算與既有分析 API。

### Task 1: Guide content contract and translations

**Files:**
- Modify: `src/i18n/zh-TW.json`, `src/i18n/en.json`, `src/i18n/es-MX.json`
- Test: existing typecheck/build

- [ ] Add complete guide keys for each page/subtab, including purpose, principle, formula, interpretation, limitations, and recommendations.
- [ ] Run `npm run build`; expected success with no missing translation type errors.
- [ ] Commit content separately.

### Task 2: Shared guide modal and navigation entry

**Files:**
- Create or modify the existing shared layout/navigation component identified by `rg -n "系統設定|systemSettings" src`
- Create: `src/components/guide/GuideModal.tsx`
- Test: `src/components/guide/GuideModal.test.tsx` if the repository test runner supports component tests

- [ ] Write a failing test for opening, closing, and rendering a supplied guide key.
- [ ] Implement the minimal modal with sections for purpose, principle, formula, interpretation, limitations, and recommendations.
- [ ] Add the navigation item below system settings and pass active page/subtab context.
- [ ] Run the focused test, then `npm run build`.
- [ ] Commit the UI integration.

### Task 3: Page/subtab mapping and regression verification

**Files:**
- Modify: the existing app tab/subtab mapping module and guide component only where required
- Test: focused guide mapping test plus existing project tests

- [ ] Write a failing test covering SPC, distribution, trend, time series, GRR, model center, Monte Carlo, validation, and reports keys.
- [ ] Implement deterministic mapping with a safe generic fallback.
- [ ] Run focused tests and full available test suite.
- [ ] Run `npm run build` and inspect `git diff --check`.
- [ ] Bump displayed version metadata to v0.8.3 in the existing single source of truth and update README/release notes.
- [ ] Commit and push after verification.
